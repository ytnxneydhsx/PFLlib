import os
import random



dir_path = "test_data/"
def generate_dataset(num_class,data_per_class,client_num):
    if not os.path.exists(dir_path):
        os.makedirs(dir_path)



def generate_data(data_simple_len,data_per_class):
    